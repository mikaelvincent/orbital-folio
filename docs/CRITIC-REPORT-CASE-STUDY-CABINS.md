**Independent critic report — Case studies cabins iteration**

**85.8/100: passes the temporary 75/100 target.** Each equally weighted area is above the temporary 6/10 floor. No blocking defect was found in the requested render changes.

This is a fresh assessment of the current render iteration. It covers spacecraft appearance, geometry/spatial navigation, the unchanged Earth/space presentation, responsive render navigation/input, and render performance/maintainability. Deferred reading interfaces, forms, admin UI and unfinished case-study workflows are excluded and incur no deductions. The former 90/100 target and 8/10 floor are not used here.

I inspected current source and actual desktop/phone captures, assessed the implementing agent’s raw browser evidence, independently ran seven bounded model lighting states, parsed the actual flight trace, verified source hashes and fetched the production render route and its assets. I did not operate the shared browser or run the obsolete broad geometry suite. The accepted model hash is `cff043883d22ed4b412156c3203897db4f6c6b316c535d1de14799a3bb997158`.

| Equal area | Score | Assessment |
| --- | ---: | --- |
| Spacecraft art and design | **8.7/10** | The requested cabin swap, nine-bay Case studies rack, brighter idle rooms and single larger hull name are clear. Physical doorway captions are upright and centered over their openings; the selected production capture no longer has the filled HTML focus overlay. The toy spacecraft remains coherent, although some lighting is flat and small physical text is truncated or difficult to read on phones. |
| Geometry and spatial navigation | **9.1/10** | Actual camera samples confirm Case studies → Projects → left passage → About → Contact. Three brightness levels work simultaneously, and full destination brightness waits for arrival. The aligned model audit passes the new rack, portal, mounting, passage and lighting checks. Portrait framing preserves primary controls while showing neighboring hull/cabin; it does not make one cabin fill every aspect ratio. |
| Earth and space atmosphere | **8.5/10** | The unchanged procedural ocean, clouds and space background remain visually coherent in current overview captures. The source hash is unchanged. The cloud treatment remains stylized and lacks full volumetric depth. No additional Earth/meteor requirements or redundant behavior testing were imposed. |
| Responsive accessible render input/navigation | **8.4/10** | At 320 pixels the dropdown displays Case studies, Projects, About and Contact in order, alongside the retained Overview choice. Tab, Escape focus return and Home navigation are evidenced, with zero horizontal overflow and zero axe violations in the sampled state. Narrow native labels ellipsize; physical doorway text remains too small to be the sole navigation method. Physical-device touch and manual screen-reader behavior were not established. |
| Render performance and maintainability | **8.2/10** | Typecheck, lint and production build pass. The production render route and all 11 referenced assets return 200. Current settled production timing is usable, with 16.7 ms median / 33.6 ms p95 over 360 intervals and zero above 50 ms; desktop/phone development samples are faster. Rendering cost and host variability remain material. Shared rack construction, explicit brightness roles and the preserved route/data alias reduce maintenance risk. |

Calculation: `(8.7 + 9.1 + 8.5 + 8.4 + 8.2) × 2 = 85.8`.

**Evidence supporting the pass**

The [idle overview](evidence/case-study-cabins/desktop-overview-idle.jpg), [hover overview](evidence/case-study-cabins/desktop-hover.jpg), [production Case studies cabin](evidence/case-study-cabins/production-case-studies.jpg) and [door-hover capture](evidence/case-study-cabins/door-hover-three-levels.jpg) show the requested arrangement and three distinct brightness states. There is one enlarged top hull name. Case studies uses the Projects rack design with nine compartments; the current three dummy entries and six equipped spare bays are acceptable for this iteration. Distinct case-study reader functionality is deferred.

The [browser record](evidence/case-study-cabins/browser-qa.json) contains `case-studies-door-hover-three-levels`: selected Case studies is **1.0**, hovered Projects **0.5**, and About/Contact **0.1**, while exterior materials remain **1.0**. My [independent model check](evidence/case-study-cabins/critic-lighting-audit.json) verifies seven states, including a conflicting hover during transit, the left passage, and arrival. It also checks the six directed side portals, the new circulation order and exactly one hull-brand record. Source inspection confirms the prior additive hover emission is removed and doorway-owned surface emission is suppressed; the recorded passage has no separate point lights.

The [159-frame actual flight trace](evidence/case-study-cabins/transit-lighting-trace.json) traverses the upper-right Case studies room, upper-left Projects room, left passage, lower-left About and lower-right Contact. Minimum camera X is **−5.599501**. Contact’s rendered brightness never exceeds **0.5** while `travelling` is true. The first arrival frame begins its smooth rise toward full brightness, and the settled snapshot reaches **1.0** with the other cabins at **0.1**. This verifies actual renderer input to the model, not merely a planned route. The same recorded renderer persists through the flight.

The aligned [model audit](evidence/case-study-cabins/model-audit.json) reports **216 slot mappings, 50 lighting cases, 16 route pairs, 108 portal views, 12 physical mounting checks and 108 passage rays**, with no visibility failures or invalid normals. The [routing/migration audit](evidence/case-study-cabins/routing-migration-audit.json) provides 29 isolated pure-helper/in-memory checks, including the `/case-studies` alias and preservation of owner-customized labels. This is supporting render-data evidence, not an admin workflow test.

The [320-pixel selected cabin](evidence/case-study-cabins/phone320-case-studies.jpg), [menu](evidence/case-study-cabins/phone320-menu.jpg) and [overview](evidence/case-study-cabins/phone320-overview.jpg) show the final phone layout. The [focused axe result](evidence/case-study-cabins/phone320-axe.json) has **zero violations and 17 passes**, with viewport and scroll width both 320. Keyboard evidence confirms Overview receives focus after opening the menu, Escape closes it and restores focus to the Case studies trigger, and Home returns to overview. The separate [native drag record](evidence/case-study-cabins/native-drag-final.json) verifies a 247-pixel mouse drag in overview: `dragged: true`, `activated: false`, with the same overview URL before and after. It began on the render surface, not a locker.

I independently verified all entries in [source-hashes.json](evidence/case-study-cabins/source-hashes.json), successful current [typecheck](evidence/case-study-cabins/typecheck.txt), [lint](evidence/case-study-cabins/lint.txt) and [build](evidence/case-study-cabins/build.txt) logs, and HTTP 200 for `/case-studies` plus its 11 referenced CSS/JavaScript files. The [production retrieval evidence](evidence/case-study-cabins/production-http.json) is packaged alongside the browser record.

**Limits retained in this assessment**

- At 320 pixels, the outer docking/array hardware is cropped, the collapsed Case studies label ellipsizes, and some physical rack/door labels are small. The open native destination menu remains readable and usable. These are current visual limitations, not deductions for deferred reading features.
- The old `desktop-case-studies-selected.jpg` contains the superseded filled focus overlay; the final production capture supplies the accepted appearance.
- The record named `production-case-studies-drag` actually ends at overview (`/`, active room `home`, zero retained drag response). Its last-gesture diagnostic describes a non-control drag. It is not counted as proof of a Case studies control drag or a settled Case studies performance sample. That window’s 33.2 ms median / 50.1 ms p95 and 22 intervals above 50 ms remain evidence of variable/interactive timing, not a reason to claim constant 60 fps.
- A retained development log records a framing RangeError during a transient viewport resize. A minimal guard now skips resize framing while width is below 240 or height below 480, leaving the existing short-viewport fallback or next valid resize to handle it. The source fix matches the observed ResizeObserver call path. The production captures predate this guard; it does not change their normal viewport path. The guard version passes final typecheck, lint and rebuild, and the restarted Worker route and all 11 referenced assets were independently retrieved successfully. No broad new reader/fallback workflow pass is inferred.
- Current settled production timing is a 1440×1000 DPR-1 host measurement, not GPU timing or a physical-phone benchmark. The 320-pixel development sample reports 16.7/17.7 ms over 360 intervals; it is labeled separately from production. No causal speed improvement is asserted.
- The Earth source is unchanged and was not put through another meteor or cloud test cycle. No fresh physical-device, OS preference-switching or manual assistive-technology pass is claimed.
- The historical external hosting HTTP 409 incident is outside this local render iteration and remains separate. This report does not certify deployment or completion of the intentionally deferred application interfaces.
